package game;

import javax.swing.*;

public class LoadGame {
    protected JPanel panel = new JPanel();
    private JButton[] loadGameButtons;

    public LoadGame(JFrame jFrame) {
        jFrame.add(panel);
    }
}
